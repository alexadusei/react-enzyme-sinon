import React from 'react'
import ReactDOM from 'react-dom'
import {MessageTextContainer} from 'containers'

ReactDOM.render(
  React.createElement(MessageTextContainer),
  document.getElementById('app')
)
